import Gutter from 'components/Gutter'

export default function PrizesLayout({ children }) {
  return (
    <main className="min-h-screen bg-gradient-to-tr from-red-800 to-blue-900 pb-12 pt-24">
      {children}
    </main>
  )
}
