import { createSlice } from '@reduxjs/toolkit'

const initialState = {
  game: null
}

const gameSlice = createSlice({
  name: 'game',
  initialState,
  reducers: {
    
  },
})


export default gameSlice.reducer
